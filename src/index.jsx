import Margin, {m, mx, my, mt, mr, mb, ml} from './Margin';
import Padding, {p, px, py, pt, pr, pb, pl} from './Padding';

export {
  Margin, m, mx, my, mt, mr, mb, ml,
  Padding, p, px, py, pt, pr, pb, pl
}
